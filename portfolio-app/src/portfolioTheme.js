import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#111827',
    },
    secondary: {
      main: '#c749c1',
    },
    background: {
      default: '#111827',
    },
  },
});

export default theme;
